// Import modul atau library yang diperlukan
// Misalnya, jika menggunakan framework atau library seperti React atau Angular, Anda bisa menyesuaikan impor sesuai kebutuhan.

// Contoh fungsi untuk menampilkan alert atau pesan kepada pengguna
function showAlert(message) {
  // Simpan logika atau fungsi untuk menampilkan pesan atau alert sesuai dengan platform atau framework yang Anda gunakan.
  // Misalnya, di React Native, Anda bisa menggunakan Alert.alert() untuk menampilkan alert.

  console.log(`Menampilkan pesan: ${message}`);
  // Misalnya, di React Native:
  // Alert.alert('Informasi', message);
}

